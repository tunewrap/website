# TuneWrap Track INBOX

Place one prepared track package folder here, then run:

```bash
npm run tracks:import
```

The importer validates every package before moving anything into `content/tracks/`.
